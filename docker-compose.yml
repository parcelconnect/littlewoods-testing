version: '2'
services:
  web:
    build: .
    command: python3 manage.py runserver 8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=idv.settings
      - DEBUG=True
    depends_on:
      - db
    env_file: .env
  db:
    image: postgres
